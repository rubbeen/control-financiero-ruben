# Acciones manuales en Firebase Console

1. Verificar que `ribenp7@gmail.com` tenga `emailVerified` y copiar su UID.
2. Publicar manualmente `firestore.rules.transition` durante una ventana aprobada.
3. Ejecutar primero dry-run y luego la migracion real con credenciales externas.
4. Comparar conteos, saldos y movimientos `needs_review`.
5. Probar la app v1.3.0 con el arbol por UID.
6. Publicar manualmente `firestore.rules` e indices finales.
7. Revisar que no exista registro publico y que Email/Password sea el unico proveedor requerido.
8. Evaluar App Check gradualmente sin enforcement inicial.

Ninguna de estas acciones fue ejecutada por Codex.
